name: Deploy admin-web to EC2

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: ap-south-1
      ECR_REPO: ${{ secrets.ECR_REPO_ADMIN_WEB }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        id: image
        run: |
          IMAGE_URI="${ECR_REPO}:${GITHUB_SHA}"
          docker buildx build \
            --platform linux/amd64 \
            --cache-from type=registry,ref="${ECR_REPO}:latest" \
            --cache-to type=inline \
            --build-arg NEXT_PUBLIC_API_URL="${{ secrets.NEXT_PUBLIC_API_URL }}" \
            --build-arg NEXT_PUBLIC_PANEL_URL="${{ secrets.NEXT_PUBLIC_PANEL_URL }}" \
            --build-arg NEXT_PUBLIC_GATEWAY_ORIGIN="${{ secrets.NEXT_PUBLIC_GATEWAY_ORIGIN }}" \
            --build-arg NEXT_PUBLIC_FIREBASE_API_KEY="${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}" \
            --build-arg NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}" \
            --build-arg NEXT_PUBLIC_FIREBASE_PROJECT_ID="${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}" \
            --build-arg NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}" \
            --build-arg NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}" \
            --build-arg NEXT_PUBLIC_FIREBASE_APP_ID="${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}" \
            --build-arg NEXT_PUBLIC_FIREBASE_VAPID_KEY="${{ secrets.NEXT_PUBLIC_FIREBASE_VAPID_KEY }}" \
            -t "$IMAGE_URI" \
            -t "${ECR_REPO}:latest" \
            --push .
          echo "registry=$(echo "$ECR_REPO" | cut -d'/' -f1)" >> "$GITHUB_OUTPUT"

      # Deploys over SSM instead of SSH: no key secret, no open port 22. Commands
      # run as root on the instance (no sudo); the instance role provides ECR auth.
      - name: Deploy on EC2 via SSM
        env:
          # Prod instance. Update if the instance is ever replaced.
          EC2_INSTANCE_ID: i-0cfd39b4452e87ef1
        run: |
          set -euo pipefail
          CMD_ID=$(aws ssm send-command \
            --instance-ids "$EC2_INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --comment "deploy admin-web ${{ github.sha }}" \
            --parameters '{
              "commands": [
                "set -euo pipefail",
                "aws ecr get-login-password --region ${{ env.AWS_REGION }} | docker login --username AWS --password-stdin ${{ steps.image.outputs.registry }}",
                "cd /home/ec2-user",
                "docker compose -f docker-compose.prod.yml --env-file .env.prod pull admin-web",
                "docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --remove-orphans",
                "docker image prune -f || true"
              ],
              "executionTimeout": ["900"]
            }' \
            --query 'Command.CommandId' --output text)
          echo "SSM command: $CMD_ID"
          STATUS=Pending
          for _ in $(seq 1 90); do
            STATUS=$(aws ssm get-command-invocation --command-id "$CMD_ID" \
              --instance-id "$EC2_INSTANCE_ID" --query 'Status' --output text 2>/dev/null || echo Pending)
            case "$STATUS" in Success|Failed|Cancelled|TimedOut) break ;; esac
            sleep 10
          done
          aws ssm get-command-invocation --command-id "$CMD_ID" --instance-id "$EC2_INSTANCE_ID" \
            --query '{Status: Status, Stdout: StandardOutputContent, Stderr: StandardErrorContent}' --output json
          test "$STATUS" = "Success"
